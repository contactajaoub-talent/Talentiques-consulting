import type { Metadata } from 'next';
import AdminShell,{AdminCard,MoneyMap} from '@/components/affiliate/admin/AdminShell';
import { requireAdminSession } from '@/lib/affiliate/admin-auth';
import { adminOverview, payoutReady } from '@/lib/affiliate/admin-supabase';
export const metadata:Metadata={title:'Vue générale affiliés',robots:{index:false,follow:false}};export const dynamic='force-dynamic';
export default async function Page(){await requireAdminSession();const [raw,ready]=await Promise.all([adminOverview(),payoutReady()]);const data=raw&&typeof raw==='object'?raw as Record<string,unknown>:{};const clicks=Number(data.clicks30d||0),sales=Number(data.sales30d||0),active=Number(data.activeAffiliates||0);const commissions=data.commissions&&typeof data.commissions==='object'?data.commissions as Record<string,unknown>:{};const byStatus=(status:string)=>({EUR:commissions[`${status}_EUR`],USD:commissions[`${status}_USD`]});const revenue=data.revenue30d&&typeof data.revenue30d==='object'?data.revenue30d as Record<string,unknown>:{};const average={EUR:Number(revenue.EUR||0)/(active||1),USD:Number(revenue.USD||0)/(active||1)};return <AdminShell title="Vue générale" subtitle="Santé opérationnelle du programme d’affiliation."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><AdminCard label="Candidatures en attente" value={String(data.pendingApplications||0)}/><AdminCard label="Affiliés actifs" value={String(active)}/><AdminCard label="Affiliés suspendus" value={String(data.suspendedAffiliates||0)}/><AdminCard label="Clics — 30 jours" value={clicks}/><AdminCard label="Ventes éligibles — 30 jours" value={sales}/><AdminCard label="Conversion affiliée" value={`${clicks?Math.round(sales/clicks*10000)/100:0}%`}/><AdminCard label="Revenu — 30 jours" value={<MoneyMap value={revenue}/>}/><AdminCard label="Revenu moyen / actif" value={<MoneyMap value={average}/>}/><AdminCard label="Commissions en attente" value={<MoneyMap value={byStatus('pending')}/>}/><AdminCard label="Commissions disponibles" value={<MoneyMap value={byStatus('available')}/>}/><AdminCard label="Commissions payées" value={<MoneyMap value={byStatus('paid')}/>}/><AdminCard label="Soldes prêts au paiement" value={ready.length}/></div><section className="mt-8 rounded-3xl border border-slate-200 bg-white p-6"><h2 className="font-heading text-2xl font-bold">Santé du programme</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><AdminCard label="Actifs avec clics" value={String(data.activeWithClicks||0)}/><AdminCard label="Affiliés avec vente" value={String(data.affiliatesWithSales||0)}/><AdminCard label="Candidatures > 3 jours" value={String(data.oldPending||0)}/><AdminCard label="Obligations à venir" value={ready.length}/></div></section></AdminShell>}
