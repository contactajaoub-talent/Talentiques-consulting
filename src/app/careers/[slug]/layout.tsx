import { localizedMetadata } from '@/lib/i18n';
import type { ReactNode } from 'react';
export const metadata = localizedMetadata("/careers/commerciale-prospection-closing", "Chargée d’Acquisition & Développement Client", "Découvrez le poste commercial TalentiQues et candidatez en ligne.");
export default function Layout({ children }: { children: ReactNode }) { return children; }
