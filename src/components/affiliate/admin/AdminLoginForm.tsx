'use client';
import { FormEvent, useState } from 'react';
export default function AdminLoginForm(){const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);const email=String(new FormData(event.currentTarget).get('email')||'');const response=await fetch('/api/affiliate/admin/auth/request-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const result=await response.json();setMessage(result.message||'Vérifiez votre boîte de réception.');setLoading(false);}return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-sm font-bold">Adresse administrateur</span><input className="input" type="email" name="email" required autoComplete="email" /></label><button disabled={loading} className="min-h-14 w-full rounded-xl bg-[#0683C9] font-bold text-white disabled:opacity-60">{loading?'Envoi…':'Recevoir le lien sécurisé'}</button>{message&&<p className="rounded-xl bg-sky-50 p-4 text-sm text-sky-900">{message}</p>}</form>}
