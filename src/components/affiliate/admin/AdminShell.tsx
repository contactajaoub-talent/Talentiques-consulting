import Link from 'next/link';
import { CircleDollarSign, FileWarning, LayoutDashboard, LogOut, UserCheck, Users, WalletCards } from 'lucide-react';
export default function AdminShell({children,title,subtitle}:{children:React.ReactNode;title:string;subtitle?:string}){const links=[['/admin/affiliates','Vue générale',LayoutDashboard],['/admin/affiliates/applications','Candidatures',UserCheck],['/admin/affiliates/affiliates','Affiliés',Users],['/admin/affiliates/commissions','Commissions',CircleDollarSign],['/admin/affiliates/payouts','Paiements',WalletCards],['/admin/affiliates/risk','Signaux à vérifier',FileWarning]] as const;return <div className="min-h-screen bg-slate-50 text-slate-950"><aside className="fixed inset-y-0 left-0 hidden w-64 bg-[#071b2b] p-6 text-white lg:flex lg:flex-col"><Link href="/admin/affiliates" className="font-heading text-2xl font-bold">TalentiQues</Link><p className="mt-2 text-xs font-bold tracking-[.16em] text-sky-300">ADMIN AFFILIÉS</p><nav className="mt-9 space-y-1">{links.map(([href,label,Icon])=><Link key={href} href={href} className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold text-slate-300 hover:bg-white/10 hover:text-white"><Icon size={18}/>{label}</Link>)}</nav><form action="/api/affiliate/admin/auth/logout" method="post" className="mt-auto"><button className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold text-slate-300"><LogOut size={18}/>Déconnexion</button></form></aside><main className="lg:ml-64"><header className="border-b border-slate-200 bg-white px-4 py-4 lg:hidden"><div className="flex gap-3 overflow-x-auto">{links.map(([href,label])=><Link key={href} href={href} className="whitespace-nowrap rounded-full bg-slate-100 px-3 py-2 text-xs font-bold">{label}</Link>)}</div></header><div className="mx-auto max-w-[1500px] px-4 py-8 sm:px-7 lg:px-10"><div className="mb-8"><p className="text-xs font-bold tracking-[.16em] text-[#0683C9]">TALENTIQUES OPERATIONS</p><h1 className="mt-3 font-heading text-3xl font-bold sm:text-5xl">{title}</h1>{subtitle&&<p className="mt-3 text-slate-600">{subtitle}</p>}</div>{children}</div></main></div>}
export function AdminCard({label,value}:{label:string;value:React.ReactNode}){return <div className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm font-semibold text-slate-500">{label}</p><div className="mt-3 font-heading text-3xl font-bold">{value}</div></div>}
export function AdminTable({headers,children}:{headers:string[];children:React.ReactNode}){return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="min-w-[850px] w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{headers.map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody>{children}</tbody></table></div>}
export function MoneyMap({value}:{value:unknown}){const map=value&&typeof value==='object'?value as Record<string,unknown>:{};return <span className="flex flex-col">{Object.entries(map).length?Object.entries(map).map(([currency,amount])=><span key={currency}>{currency} {Number(amount).toFixed(2)}</span>):'—'}</span>}
